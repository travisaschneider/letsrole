<ul class="nav nav-tabs" id="extra-{{ key }}" role="tablist">
    <li class="nav-item">
        <a class="nav-link active" id="extra-{{ key }}-bars-tab" data-toggle="tab" href="#extra-{{ key }}-bars" role="tab" aria-controls="extra-{{ key }}-bars" aria-selected="true">{{ __('Bars') }}</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" id="extra-{{ key }}-aura-tab" data-toggle="tab" href="#extra-{{ key }}-aura" role="tab" aria-controls="extra-{{ key }}-aura">{{ __('Status') }}</a>
    </li>
</ul>

<div class="tab-content mt-3" id="extra-{{ key }}-content">
    <div class="tab-pane fade show active" id="extra-{{ key }}-bars" role="tabpanel" aria-labelledby="extra-{{ key }}-bars-tab">
        {{ html.bars|safe }}
    </div>
    <div class="tab-pane fade" id="extra-{{ key }}-aura" role="tabpanel" aria-labelledby="extra-{{ key }}-aura-tab">
        {{ html.aura|safe }}
    </div>
</div>

<div class="toolbar d-flex">
    <a href="#" class="save-btn"><i class="fas fa-check"></i> {{ __('Save & apply') }}</a>
</div>