<div class="popin {{ className }}" id="{{ id }}" style="width: {{ width }}px">
    <header class="d-flex flex-row align-items-center">
        <h3 class="mr-auto">{{ title }}</h3>
        <a href="#" class="close"><i class="fas fa-times"></i></a>
    </header>
    <main>
        {{ html|safe }}

        <div class="mt-3 text-right" id="prompt-actions">
            <a href="#" class="btn btn-secondary btn-continue">{{ __('Continue') }} <i class="fas fa-arrow-right"></i></a>
        </div>
    </main>
    <span class="resize">◢</span>
</div>