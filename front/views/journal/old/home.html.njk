<div class="encyclopedia-home">
    <header class="d-flex">
        <div class="flex-grow-1 search-container">
            <input type="search" class="form-control encyclopedia-search" placeholder="{{ __('Find something…') }}">
            <a class="erase-btn" href="#"><i class="fas fa-times-circle"></i></a>
        </div>

        <div class="journal-visibility dropdown mr-2">
            <a class="dropdown-toggle" href="#" id="journalVisibilityDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-map-pin"></i> {{ __('This table') }}
            </a>
            <div class="dropdown-menu" aria-labelledby="journalVisibilityDropdown">
                <a class="dropdown-item" href="#" data-visibility="table"><i class="fas fa-map-pin"></i> {{ __('This table') }}</a>
                <a class="dropdown-item" href="#" data-visibility="all"><i class="far fa-globe"></i> {{ __('All tables') }}</a>
            </div>
        </div>
    </header>

    <div class="journal-folders"></div>

    <div class="journal-header"></div>

    <div class="journal-items">
        <p class="text-center">
            <i class="fas fa-circle-notch fa-spin"></i>
            {{ __('Loading...') }}
        </p>
    </div>

    <form id="upload-pdf-container" onsubmit="return false" method="post" class="invisible">
        <input type="file" id="pdf-file" name="media[]" accept="application/pdf">
    </form>

    <footer class="create-menu">
        <div id="progress-container"></div>
        <div class="d-flex justify-content-around toolbar">
            <a class="new-journal-btn" href="#"><i class="fas fa-file-plus"></i> {{ __('New entry') }}</a>
            <a class="upload-pdf-btn" href="#"><i class="fas fa-file-pdf"></i> {{ __('Upload PDF') }}</a>
            <a class="new-folder-btn" href="#"><i class="fas fa-folder-plus"></i> {{ __('New folder') }}</a>
        </div>
    </footer>
</div>